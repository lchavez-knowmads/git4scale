default:
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: database
      schema: main
      schemas_and_paths:
        main: ./weather_air_quality.db
      schema_directory: .
  target: dev